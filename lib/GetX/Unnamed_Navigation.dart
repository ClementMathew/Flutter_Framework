import 'package:flutter/material.dart';

class UnNamedGetX extends StatelessWidget {
  const UnNamedGetX({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}